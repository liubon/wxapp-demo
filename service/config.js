const devConfig = {
  baseUrl: 'https://dev.com',
  timeout: 3000,
  staticUrl:"https://dev.com.com"
};
const proConfig = {
  timeout: 5000,
  baseUrl: "https://pro.com"
};
const config = devConfig
export const staticUrl = "https://test-zml.bhfae.com/"
export default config;
